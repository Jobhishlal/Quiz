// Configuration for API
// In a real app, this would read from import.meta.env.VITE_API_URL
const API_CONFIG = {
    baseURL: 'http://localhost:5000/api', 
    timeout: 10000,
};

export default API_CONFIG;
